------------------------------------------------------------------------
--[[ State ]]--
-- Used
------------------------------------------------------------------------
local State = torch.class("dp.State")

function State:__init()
   
end

-- accumulates both states attributes into a single state
function State:join(state)
   
end

function State:emptyClone()
   
end
